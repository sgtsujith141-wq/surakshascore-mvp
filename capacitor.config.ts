import type { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.sentinel.security',
  appName: 'SurakshaScore MVP',
  webDir: 'dist'
};

export default config;
